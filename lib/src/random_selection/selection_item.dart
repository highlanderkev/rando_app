/// A placeholder class that represents an entity or model.
class SelectionItem {
  const SelectionItem(this.id, this.details, this.info, this.routeName);

  final int id;
  final String details;
  final String info;
  final String routeName;
}
